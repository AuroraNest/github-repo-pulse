{
  "name": "@repopulse/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start": "tsx src/index.ts"
  },
  "dependencies": {
    "@repopulse/core": "workspace:*",
    "@repopulse/db": "workspace:*",
    "tsx": "^4.20.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
