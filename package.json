{
  "name": "repopulse",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "dev:web": "pnpm --filter @repopulse/web dev",
    "dev:worker": "pnpm --filter @repopulse/worker dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "check:logic": "node --test packages/core/src/checks/daily-delta.test.ts"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.8.3"
  }
}
